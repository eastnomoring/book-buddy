# AI 伴学系统 —— 项目交接文档

> **文档目的**：本文档是项目前期调研与设计讨论的完整交接记录。接手的智能体/开发者读完本文档后，应能不经额外提问即可继续推进项目。
> **文档日期**：2026-07-23
> **当前阶段**：设计讨论已完成，尚未编写任何代码。下一步是起草 README 框架与仓库结构，然后实现第一个里程碑（MVP）。

---

## 1. 项目背景与初衷

### 1.1 发起人需求（原话要点）

- 发起人想做一套**伴学系统**，利用大模型帮助学习"目之所及的知识"，核心场景是**学习一本具体的书**（示例书目：《普林斯顿概率论读本》）。
- 看书时的疑问通过大模型解答，并要求可调用 **MCP（Model Context Protocol）** 工具。
- 需要**四种输入输出模态**：
  - 视觉输入：摄像头拍书页（台式机摄像头、Surface 摄像头等）/ 屏幕截图（读 PDF 时）
  - 视觉输出：公式渲染、图示、模拟动画
  - 语音输入：ASR，手不离开书直接提问
  - 语音输出：TTS，像家教一样讲解
  - 文字输入输出：聊天界面
- 初期要求"全平台可移植"，因此编程语言/框架选型是关键决策点。
- 计划将项目**开源发布到 GitHub**。

### 1.2 项目定位（一句话）

**"Read any book with an AI companion" —— 面向自学者读硬书的 AI 伴读系统**，区别于市面上泛滥的 "AI tutor"（面向学生写作业场景）。

---

## 2. 核心设计决策（已达成共识）

### 2.1 分层架构原则

> 最重要的架构洞察：**"大模型调用 + 学习逻辑"占系统 90% 的价值，摄像头等硬件采集只是占 10% 的薄适配层。** 不要让硬件访问绑架整体架构。

```
┌─────────────────────────────────┐
│  UI 层（拍照按钮、对话、展示）      │  ← 平台相关
├─────────────────────────────────┤
│  采集层（摄像头/相册/截图）          │  ← 平台相关（很薄）
├─────────────────────────────────┤
│  核心层（提示词工程、多模态调用、     │  ← 纯 HTTP 调用，全平台通用
│  知识库、出题/讲解/复习逻辑）        │
└─────────────────────────────────┘
```

### 2.2 推荐技术架构：本地 Web 应用（Python 后端 + 浏览器前端）

```
┌──────────────── 浏览器（UI 层，零平台代码） ────────────────┐
│  摄像头 getUserMedia │ 麦克风 Web Audio │ 扬声器/TTS 播放     │
│  聊天界面 │ KaTeX 公式 │ 图表渲染 │ 截图/屏幕共享 getDisplayMedia│
└──────────────────────┬───────────────────────────────────┘
                       │ WebSocket / HTTP（都跑在 localhost）
┌──────────────────────▼────────── Python 后端（核心层） ──────┐
│  ┌─────────────────────────────────────────────────────┐   │
│  │ Agent 编排：对话状态机、阅读进度、提示词工程            │   │
│  ├─────────────────────────────────────────────────────┤   │
│  │ 多模态管道：ASR(whisper) → LLM(多模态) → TTS          │   │
│  ├─────────────────────────────────────────────────────┤   │
│  │ 书籍知识库：PDF 解析 → 分块 → 向量检索（RAG）           │   │
│  ├─────────────────────────────────────────────────────┤   │
│  │ MCP Client：代码执行 / Wolfram / Anki / 笔记 / 搜索    │   │
│  └─────────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────────┘
```

**选型理由（四条）：**

1. **浏览器免费解决全部四种 I/O**：摄像头、麦克风、扬声器、屏幕捕获在浏览器里都是统一 API（getUserMedia / Web Audio / getDisplayMedia），台式机、Surface、Mac、iPad 打开网页即用——"全平台摄像头调用"问题直接消失，零平台代码。
2. **Python 是 AI 编排和 MCP 生态的最短路径**：MCP 官方 SDK 有 Python 版；whisper、TTS、向量库、PDF 解析生态最成熟。
3. **前后端都在 localhost，天然可移植**：后端可打包为单个可执行文件；前端以后也可部署到云端供 iPad 访问。
4. **未来可平滑演进**：如需独立 App，把浏览器壳换成 Tauri 或 Flutter 套壳即可，核心层不用改。

### 2.3 被否决/降级的备选方案

| 方案 | 结论 | 原因 |
|---|---|---|
| Flutter (Dart) | 备选（第二阶段若必须上原生移动端再考虑） | 单代码库覆盖桌面+移动，但对个人桌面优先场景过重 |
| Electron/Tauri | 暂不上 | 初期浏览器直接够用； Electron 体积大且到不了移动端 |
| 纯 Python GUI (PySide6 等) | 否决 | GUI 生态一般，打包分发痛苦 |
| .NET (MAUI/Avalonia) | 否决 | 离开 Windows 生态受限 |
| OpenAI Realtime 类一体化语音方案 | 语音链路初期推荐用云端 API | 最省事；追求隐私再换本地 whisper + Kokoro TTS |

---

## 3. 交互闭环设计（一次学习瞬间的完整流程）

```
用户读到《普林斯顿概率论》第 3 章，对条件期望的证明有疑问
        │
        ▼
① 语音输入："这个 tower property 为什么成立？"（手不离开书）
② 视觉输入：摄像头拍下当前页（或截图，如果读的是 PDF）
        │
        ▼
大模型拿到【书页图像 + 问题语音转文字 + 该章知识上下文（RAG）】
        │
        ▼
③ 文字输出：逐步讲解，KaTeX 渲染公式
④ 语音输出：同时用语音讲一遍（像家教坐在旁边）
⑤ 视觉输出：画出图示 / 跑蒙特卡洛模拟验证（通过 MCP 调代码执行）
        │
        ▼
沉淀：自动生成一张 Anki 卡片 + 一条笔记（通过 MCP 写入）
```

---

## 4. 三个核心技术难点（摄像头不在其中）

1. **书籍知识库（RAG）—— 决定回答质量的灵魂。**
   将教材 PDF 按章节解析、分块、向量化。用户提问时 LLM 手里要有这本书自己的定义、记号和例题，回答要用**与书中一致的符号体系**，而不是泛泛的维基百科式解释。数学书注意公式解析，可用 marker / nougat 类工具做 PDF → 结构化文本。

2. **"当前页"定位 —— "伴学"区别于"问答机器人"的核心体验。**
   摄像头拍一页 → 视觉模型识别出是第几页/哪一节 → 自动拉出该章上下文和用户的历史疑问，营造"它知道你在读哪儿"的感觉。

3. **语音链路延迟。**
   边说边问的流畅感取决于 ASR→LLM→TTS 全链路延迟，建议全部走流式（streaming），LLM 边生成边送 TTS，目标感知延迟 1-2 秒。

**实践细节**：图片上传前本地压缩到 ~1MB 以内，多模态调用又快又省钱。

---

## 5. MCP 工具箱规划（学习场景的杀手锏组合）

| MCP 工具 | 用途 | 生态现状 |
|---|---|---|
| **代码执行 MCP** | 概率论场景杀手锏："大数定律是什么感觉？" → 直接模拟抛硬币 10 万次画图 | 有现成实现 |
| **Wolfram / 符号计算** | 验证积分、求期望方差 | 有现成实现 |
| **Anki MCP** | 每学完一节自动生成抽认卡，间隔重复 | **生态最成熟**：ankimcp/anki-mcp-server（~396 star）+ Anki 插件 + headless Docker |
| **笔记 MCP**（Obsidian / 本地 markdown） | 讲解自动沉淀成带公式的笔记，按章节归档 | 有现成实现 |
| **搜索 MCP** | 书上没讲透的背景知识自动补充 | 有现成实现 |

**开源故事亮点**："工具能力全靠 MCP 接入，社区可即插即用"——README 中的重要卖点，契合当前 MCP 生态热度。

---

## 6. 里程碑路线图

### 里程碑 1（MVP，预计 2-3 天）—— 验证价值假设

**最窄闭环：截图当前屏幕（读 PDF 场景）→ 文字提问 → 文字回答，且回答带 RAG 上下文。**

- 不做语音、不做摄像头、不做 MCP。
- 目的：验证"带书籍上下文的讲解"是否真的比直接问 ChatGPT 好 —— 这是整个项目的价值假设。

### 里程碑 2-5（按优先级逐个叠加）

1. 语音输入（ASR + 流式链路）
2. MCP 代码执行（概率模拟）
3. 摄像头拍书页 + 当前页定位
4. Anki 卡片自动生成
5. 语音输出（TTS 流式）

---

## 7. 竞品调研结果（2026-07-23，基于 GitHub 搜索）

### 7.1 结论先行

**有类似项目，但没有"做成"的 —— 该方向目前没有明星项目，处于空窗期。**

- 单个零件全部存在且不少已成熟（Anki MCP、Realtime API、RAG 框架）；
- 完整系统没人做：没有项目把「书籍知识库 + 视觉定位当前页 + 语音对话 + MCP 工具链」编排成连贯的伴学体验；
- 本项目的三个差异化设计 —— "它知道你在读哪一页"、"用书中符号体系回答"、"学完自动沉淀 Anki 卡片" —— 没有任何现有项目覆盖。

### 7.2 分类清单

**① 摄像头/视觉 + 语音实时伴读（与本项目最接近）**

| 项目 | Star | 说明 |
|---|---|---|
| openai/openai-realtime-console | ~3.6k | OpenAI 官方 Realtime API 演示：摄像头画面+语音实时对话。技术上最接近"看着书教你"，但只是 API demo，无任何学习逻辑。**音视频链路值得参考** |
| Suyash018/Realtime-Tutor-with-GPT-4o | ~13 | 截图分析作业 + 语音讲解，个人 demo 级 |
| MindMend（情绪感知家教） | ~0 | 摄像头分析表情调整教学，几乎无代码 |

**② 教材 RAG / "和课本对话"**

| 项目 | Star | 说明 |
|---|---|---|
| CogitoNTNU/TutorAI | ~37 | 多格式教材 RAG，带课程引用 |
| drpwchen/textbook-to-note | ~65 | PDF 教材 → 可检索知识库（带插图出处）。**教材解析值得参考** |
| run-llama/study-llama | ~23 | LlamaCloud 学习伴侣 |

**③ 语音学习助手**：基本为 0-2 star 个人项目（Whisper + gTTS + Ollama 拼装），无成熟品。

**④ MCP 工具链（最成熟的部分）**

| 项目 | Star | 说明 |
|---|---|---|
| ankimcp/anki-mcp-server | ~396 | TypeScript，MIT。Anki MCP 生态最完整（服务器+插件+Docker） |
| nailuoGG/anki-mcp-server | ~244 | 通过 AnkiConnect 对接 |
| scorzeth/anki-mcp-server | ~184 | JavaScript，MIT |

> 注意：调研中发现很多类似项目**未挂开源 License**，导致他人不敢使用 —— 本项目挂上宽松 License 本身就是传播优势。

### 7.3 可"站在肩膀上"的三个参考

1. `openai-realtime-console` → 音视频实时链路
2. `textbook-to-note` → 教材 PDF 解析与知识库构建
3. `ankimcp/anki-mcp-server` → Anki 对接方式

---

## 8. 开源发布策略建议

1. **定位话术**：README 第一句就要砸出差异 —— 不是 "AI tutor"（面向写作业的学生），而是 "read any book with an AI companion"（面向读硬书的自学者）。
2. **License**：建议 MIT 或 Apache-2.0（宽松 License = 传播优势）。
3. **命名**：避免 "AI tutor" 类泛滥平庸的名字，围绕"伴读/共读"意象起名。名字尚未确定。
4. **卖点叙事**：MCP 可插拔工具生态 + "它知道你在读哪一页" + 学习自动沉淀（Anki/笔记）。

---

## 9. 用户已确认的关键决策（2026-07-23）

| # | 问题 | 用户答案 | 影响 |
|---|---|---|---|
| 1 | 书的形式 | **主要读纸质书** | 摄像头是主入口；MVP 先用手动上传照片绕过硬件调试，快速验证 |
| 2 | 大模型 API | **国产大模型（通义千问/DeepSeek）** | 使用 Qwen-VL（多模态）+ Qwen-Audio（语音），或 DeepSeek-VL；国内访问快、中文友好 |
| 3 | 语音核心程度 | **非常重要 —— "手不离书、动口提问"是刚需** | 语音链路从第一天纳入架构，流式 ASR+TTS，里程碑优先级提前 |
| 4 | 项目名称 | 尚未确定 | 待定 |
| 5 | 目标用户 | 先纯自用 | 暂不考虑多用户/移动端 |

> **接手者注意**：基于上述决策，里程碑 1 应改为：**摄像头拍照 + 语音提问 + 带书籍上下文的回答**（不再是"先截图后语音"）。

---

## 10. 尚未确认的开放问题（接手者需向用户确认）

*（以上关键问题已全部确认，目前无阻塞项）*

如后续有新需求变化，请随时更新本文档。

---

## 10. 接手者下一步行动清单

1. [ ] 向用户确认第 9 节的开放问题（至少 #1 和 #2，阻塞实现）
2. [ ] 起草 GitHub 仓库的 README 框架（含定位话术、架构图、Roadmap、MCP 工具列表、License）
3. [ ] 确定仓库目录结构（建议：`backend/` Python FastAPI + `frontend/` Web UI + `docs/`）
4. [ ] 实现里程碑 1 MVP：
   - [ ] FastAPI 后端骨架 + LLM API 封装
   - [ ] 教材 PDF 解析与向量化（RAG 管道，先试 marker 解析数学公式）
   - [ ] 浏览器前端：截图（getDisplayMedia）+ 文字聊天 + KaTeX 渲染
   - [ ] 端到端验证：截一页书 → 提问 → 带 RAG 上下文的回答
5. [ ] 验证价值假设：对比"带书籍上下文的回答" vs "直接问裸模型"的质量差异，向用户汇报

---

## 11. 已完成的工作（2026-07-23）

### 11.1 项目结构已创建

```
ai-study-companion/
├── README.md           # 项目主文档（定位、功能、架构、快速开始）
├── HANDOFF.md          # 本交接文档
├── ARCHITECTURE.md     # 目录结构详细说明
├── LICENSE             # MIT License
├── .gitignore
├── backend/
│   ├── README.md
│   ├── main.py         # FastAPI 入口（骨架，含 CORS 和健康检查）
│   └── requirements.txt
├── frontend/
│   └── README.md
├── docs/               # 文档目录（待填充）
└── scripts/            # 辅助脚本目录（待填充）
```

### 11.2 核心决策已锁定

- 书籍形式：纸质书为主 → 摄像头是主入口
- LLM API：通义千问（Qwen-VL + Qwen-Audio）或 DeepSeek
- 语音交互：刚需，从第一天纳入架构

### 11.3 竞品调研完成

- 确认该方向无明星项目，空白点明确
- 锁定三个可参考项目（realtime-console、textbook-to-note、ankimcp）

---

## 12. 接手者下一步行动清单（更新）

### 立即可做（无阻塞）

1. [ ] 初始化 Git 仓库并做首次提交
   ```bash
   cd /Users/yaozhiyuan/ZCodeProject/ai-study-companion
   git init
   git add .
   git commit -m "feat: 初始化项目结构和文档"
   ```

2. [ ] 创建 GitHub 仓库并推送（需确认项目名称）
   - 当前占位名称：`book-buddy`
   - 建议用户确定最终名称后修改

3. [ ] 实现后端骨架：注册基础路由
   - `app/routers/chat.py` - 对话接口
   - `app/routers/voice.py` - 语音接口
   - `app/routers/book.py` - 书籍上传/解析

4. [ ] 实现前端骨架：
   - 选择框架（Vue 3 或 React）
   - 初始化 Vite 项目
   - 实现摄像头组件 `Camera.vue`

### 里程碑 1 实现细节

**目标**：摄像头拍照 + 语音提问 + 带书籍上下文的回答

**后端任务**：
- [ ] 接入 Qwen-VL API（图像 + 文本多模态）
- [ ] 接入 Qwen-Audio 或阿里云 ASR/TTS
- [ ] 实现书籍 PDF 解析（marker + 按章节分块）
- [ ] 实现向量检索（Chroma）
- [ ] 流式响应（SSE）

**前端任务**：
- [ ] 摄像头组件（getUserMedia）
- [ ] 语音录制组件（Web Audio API）
- [ ] 对话界面（消息列表 + KaTeX 渲染）
- [ ] 流式显示回答

---

## 13. 讨论过程中的关键原话（保留语境）

- "我做这个系统初衷是帮助我学习我目之所及的知识，简单说，就是帮我学习一本书，比如普林斯顿的概率论读本，看书的时候疑问可以通过大模型，调用 mcp 解答，所以还需要视觉输入输出，语音输入输出，文字输入输出等"
- "一开始设计应该是可以全平台移植，选用的语言就很关键，先能调用摄像头，比如台式机的摄像头，或者 surface 的摄像头什么的"
- "我还要把这个创意放到 GitHub 上，现在有类似的项目吗"
- 用户确认：主要读纸质书、用国产大模型、语音是刚需
