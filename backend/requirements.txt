# 后端依赖（里程碑 1 最小集合）

fastapi==0.141.1
uvicorn[standard]==0.52.1
python-multipart==0.0.32
python-dotenv==1.2.2
pydantic-settings==2.14.2

# LLM & 多模态
dashscope==1.26.5  # 通义千问 SDK
openai==2.52.0  # OpenAI 兼容接口（智谱 GLM / 硅基流动 / Ollama 通用）

# RAG & 向量
chromadb==1.5.9

# PDF 解析（文本型 PDF；扫描版后续可接 OCR / marker）
pymupdf==1.28.0

# 公式渲染（服务端 matplotlib mathtext → SVG/PNG，供小程序端无 DOM 环境使用）
matplotlib==3.11.1

# 开发工具
pytest==9.1.1
httpx==0.28.1  # 生产依赖：anki.py 经 HTTP 调 AnkiConnect；测试也用它打异步接口
